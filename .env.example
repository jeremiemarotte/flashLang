POSTGRES_USER=flashlang
POSTGRES_PASSWORD=change-me
POSTGRES_DB=flashlang

# Bearer tokens — generate with `openssl rand -hex 32`. One per consumer, both required.
HERMES_TOKEN=change-me
PWA_TOKEN=change-me

# Host port the api container publishes (reachable over Tailscale).
API_PORT=8000
